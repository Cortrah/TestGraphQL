<%= form_for(@border_type) do |f| %>
  <% if @border_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@border_type.errors.count, "error") %> prohibited this border_type from being saved:</h2>

      <ul>
      <% @border_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :is_directional %><br>
    <%= f.check_box :is_directional %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
